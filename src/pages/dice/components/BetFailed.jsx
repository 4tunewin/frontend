import React from 'react';
import { Message } from 'semantic-ui-react';

const BetFailed = () => (
    <Message negative>Your bet has failed, plase try again later.</Message>
);

export default BetFailed;
